{
  "name": "tracking-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "docs"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/server build && bun run --cwd packages/react-admin build && bun run --cwd packages/extension-scroll build && bun run --cwd packages/extension-rage-click build && bun run --cwd packages/extension-replay build",
    "dev:server": "bun run --cwd examples/express-server dev",
    "dev:client": "bun run --cwd examples/react-app dev",
    "docs:dev": "bun run --cwd docs dev",
    "docs:build": "bun run --cwd docs build",
    "docs:preview": "bun run --cwd docs preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
